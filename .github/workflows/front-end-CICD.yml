name: Upload Website

on:
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: jakejarvis/s3-sync-action@master
        with:
          args: --acl private --follow-symlinks --delete
        env:
          AWS_S3_BUCKET: "anurag-cloud-resume-challenge"
          AWS_ACCESS_KEY_ID: "AKIAX6JP7W2ZS66MIGPO"
          AWS_SECRET_ACCESS_KEY: "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"
          AWS_REGION: "us-east-1"
          SOURCE_DIR: "cloud-resume-challenge"